const quantidade = 10;
let soma = 1;
for(let i = 0; i < quantidade; i++){
    soma = soma + i;
    console.log(soma);
}

console.log('ACABOU!', soma);
// variavel, validação, o que vai acontecer no final do for 